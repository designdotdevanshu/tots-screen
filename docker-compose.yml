services:
  tots-screen:
    build: .
    image: tots-screen:latest
    container_name: tots-screen-hub
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
